export default {
  plugins: {
    '@tailwindcss/postcss': {},
    // autoprefixer: {}, // autoprefixer is no longer needed in Tailwind CSS v4 as it handles prefixing automatically, but can remain if other CSS needs it. We'll leave it.
    autoprefixer: {},
  },
}
